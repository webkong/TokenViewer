<Window x:Class="TokenViewerWindows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:TokenViewerWindows.Views"
        Title="TokenViewer"
        Width="1240"
        Height="860"
        MinWidth="1000"
        MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBg}"
        Foreground="White">

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <DockPanel Grid.Row="0" Margin="0,0,0,20">
      <StackPanel>
        <TextBlock Text="TokenViewer" FontSize="30" FontWeight="SemiBold" />
        <TextBlock Text="{views:L10n usageSubtitle}" Foreground="{StaticResource MutedText}" FontSize="13" />
      </StackPanel>
    </DockPanel>

    <TabControl x:Name="Tabs" Grid.Row="1" Background="Transparent" BorderThickness="0">
      <TabItem x:Name="UsageTab" Header="{views:L10n usage}">
        <views:UsageView DataContext="{Binding Usage}" />
      </TabItem>

      <TabItem x:Name="LimitsTab" Header="{views:L10n limits}">
        <views:LimitsView DataContext="{Binding Limits}" />
      </TabItem>

      <TabItem x:Name="SettingsTab" Header="{views:L10n settings}">
        <views:SettingsView DataContext="{Binding Settings}" />
      </TabItem>

      <TabItem x:Name="AboutTab" Header="{views:L10n about}">
        <views:AboutView DataContext="{Binding Updates}" />
      </TabItem>
    </TabControl>
  </Grid>
</Window>
