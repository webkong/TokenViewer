name: TokenViewer
options:
  bundleIdPrefix: com.tokenviewer
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "16.0"

settings:
  base:
    MARKETING_VERSION: "0.3.3"
    CURRENT_PROJECT_VERSION: 30300
    SWIFT_VERSION: "5.9"
    TOKENVIEWER_RUST_TARGET: aarch64-apple-darwin

targets:
  TokenViewer:
    type: application
    platform: macOS
    sources:
      - path: TokenViewer
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tokenviewer.app
        INFOPLIST_FILE: TokenViewer/Resources/Info.plist
        CODE_SIGN_STYLE: Automatic
        LIBRARY_SEARCH_PATHS:
          - "$(PROJECT_DIR)/../core/target/$(TOKENVIEWER_RUST_TARGET)/release"
        OTHER_LDFLAGS:
          - "-force_load"
          - "$(PROJECT_DIR)/../core/target/$(TOKENVIEWER_RUST_TARGET)/release/libtokenviewer_core.a"
          - "-framework"
          - "Security"
          - "-framework"
          - "SystemConfiguration"
          - "-lz"
          - "-liconv"
        SWIFT_OBJC_BRIDGING_HEADER: "TokenViewer/Bridge/TokenViewer-Bridging-Header.h"
    entitlements:
      path: TokenViewer/Resources/TokenViewer.entitlements
    preBuildScripts:
      - name: Build Rust Core
        script: |
          export PATH="$HOME/.cargo/bin:$PATH"
          cd "$PROJECT_DIR/../core"
          cargo build --release --target "$TOKENVIEWER_RUST_TARGET"
        outputFiles:
          - "$(PROJECT_DIR)/../core/target/$(TOKENVIEWER_RUST_TARGET)/release/libtokenviewer_core.a"
  TokenViewerTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: TokenViewerTests
    dependencies:
      - target: TokenViewer
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tokenviewer.tests
        GENERATE_INFOPLIST_FILE: YES
